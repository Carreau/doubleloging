============
Installation
============

At the command line::

    $ easy_install doubleloging

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv doubleloging
    $ pip install doubleloging
